#!/bin/bash

cd catalog
vagrant up > garbage.txt
vagrant ssh -c "cd /vagrant && mysql -u root -ppassword catDB < test3-jane-1.post.2.sql"
cd ../machine1
vagrant up > garbage.txt
vagrant ssh -c "cd /vagrant && mysql -u root -ppassword testDB1 < test3-jane-1.post.1.sql"
cd ../machine2
vagrant up > garbage.txt
vagrant ssh -c "cd /vagrant && mysql -u root -ppassword testDB2 < test3-jane-1.post.1.sql"
cd ..
